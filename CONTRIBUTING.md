# 新增一支工具

## 步驟

### 1. 複製樣板

```bash
cp -r tools/_template tools/rational-method
```

資料夾名稱用**小寫英文加連字號**，會直接變成網址的一部分：
`https://evan717717.github.io/mep-toolbox/tools/rational-method/`

### 2. 寫工具

編輯 `tools/rational-method/index.html`。樣板裡有四個標記處：

| 標記 | 要改什麼 |
| --- | --- |
| ① | `<title>` 換成工具名稱 |
| ② | 眉標的分類與編號、`<h1>` 標題、一句話說明 |
| ③ | 頁尾的公式來源與假設條件 — **不可省略** |
| ④ | `calc()` 計算函式，以及輸入欄位 |

計算邏輯寫成**純函式**（只吃數字、吐數字，不碰 DOM）。這樣之後要驗算或搬到別的地方都容易。

### 3. 登錄到工具清單

打開 `assets/tools.js`，在陣列裡加一筆：

```js
{
  id: 'HYD-02',
  name: '雨水排水量估算',
  path: 'tools/rational-method/',
  summary: '合理化公式估算集水區逕流量，內建各地降雨強度公式與逕流係數表。',
  formula: 'Q = C · I · A / 360',
  tags: ['水力', '排水'],
  status: 'live',
  updated: '2026-08-20',
},
```

編號規則：`<領域碼>-<兩位數流水號>`

| 領域碼 | 範圍 |
| --- | --- |
| `HYD` | 水力、排水、給水 |
| `HVA` | 空調、通風 |
| `ELE` | 電氣、電力 |
| `FIR` | 消防 |
| `STR` | 結構 |
| `GEN` | 通用換算 |

`status` 有三種：`live` 可用、`beta` 測試中（卡片會標示）、`wip` 開發中（卡片變灰、不可點）。

`tags` 盡量沿用既有標籤，篩選列才不會爆炸。目前使用中的標籤可以直接看首頁上方的分類鈕。

### 4. 本地確認

```bash
python3 -m http.server 8000
```

打開 `http://localhost:8000`，確認：
- 首頁卡片有出現，點得進去
- 工具頁「回工具箱」按得回來
- 手機寬度（瀏覽器開發者工具切成 375px）版面不跑掉

### 5. 送出

```bash
git checkout -b feat/rational-method
git add .
git commit -m "feat(hyd-02): 新增雨水排水量估算工具"
git push origin feat/rational-method
```

到 GitHub 開 Pull Request，合併進 `main` 後約一分鐘自動上線。

---

## 上線前檢查清單

**正確性**
- [ ] 公式來源寫在頁面上（規範條號、教科書、技術手冊）
- [ ] 至少手算一組數字對答案，把這組數字寫在 PR 說明裡
- [ ] 單位標示清楚，換算過的地方有註明
- [ ] 邊界情況不會炸：輸入 0、負數、空白、超大數值時顯示「—」或明確錯誤訊息，而不是 `NaN` 或 `Infinity`

**可用性**
- [ ] 手機寬度可正常操作
- [ ] 用 Tab 鍵可以走完所有欄位，焦點看得見
- [ ] 有預設值，一打開就看得到一組完整範例，不是空白畫面

**一致性**
- [ ] 沿用 `assets/tokens.css` 的顏色變數，不要自己調色
- [ ] 有「回工具箱」按鈕
- [ ] `assets/tools.js` 已登錄，`updated` 日期正確

---

## 常見問題

**工具需要後端 API 或資料庫怎麼辦？**
這個站是純靜態的，沒有後端。需要伺服器的工具請另外開專案，然後在 `tools.js` 裡把 `path` 指向那個專案的網址即可 — 首頁一樣會列出來。

**可以用 React 或其他框架嗎？**
可以，但請自行建置，把產出的靜態檔放進工具資料夾。本 repo 不做全站建置，避免整個站被單一支工具的相依套件綁住。

**工具檔案太大怎麼辦？**
如果單一 HTML 超過約 300 KB，把 CSS 和 JS 拆成同資料夾的獨立檔案。這樣就不能整檔離線常走了，請在工具說明裡註明。

**改壞了想退回？**
GitHub 上每次 commit 都留著。到 repo 的 Commits 頁面找到上一個正常版本，按 Revert 就好。
